{
  "name": "ts_schema_spec",
  "private": true,
  "description": "Dev-only: the gem's own suite resolves the generator the same way a consuming repo does.",
  "scripts": {
    "prettier": "prettier --check --cache --ignore-unknown .",
    "prettier:write": "prettier --write --cache --ignore-unknown ."
  },
  "devDependencies": {
    "prettier": "3.9.9",
    "ts-json-schema-generator": "^2.4.0",
    "typescript": "^5.4.0"
  }
}
